#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *Return: Always return 0 after code execute
 */

void print_alphabet(void);
{	
char n;

for (n = 'a'; n <= 'z'; n++)
{
_putchar(n);

}

return (0);

}
